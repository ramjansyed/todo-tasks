name: Build and Deploy to Kubernetes

on:
  push:
    branches:
      - main
      - development

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Cache Docker layers
        uses: actions/cache@v2
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Tag Docker image
        run: |
          docker build -t todo-tasks-server:$GITHUB_SHA .
          docker tag myapps:$GITHUB_SHA ramjansyed/myapps:$GITHUB_SHA

      - name: Push Docker image to Docker Hub
        run: |
          docker push ramjansyed/myapps:$GITHUB_SHA

#  deploy:
#    runs-on: ubuntu-latest
#    needs: build
#
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v3
#
#      - name: Set up Kubeconfig
#        uses: azure/setup-kubectl@v1
#        with:
#          kubeconfig: ${{ secrets.KUBE_CONFIG }}
#
#      - name: Deploy to Kubernetes
#        run: |
#          kubectl set image deployment/your-deployment-name your-container-name=your-image-name:$GITHUB_SHA
#          kubectl rollout status deployment/your-deployment-name
