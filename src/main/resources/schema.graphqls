type Todo {
    id: ID!
    title: String!
    description: String
    subtasks: [SubTask]
    completed: Boolean
}

type SubTask {
    id: ID!
    title: String!
    description: String
    todo_id: ID
    completed: Boolean
}

type Query {
    todos: [Todo]
    todo(id: ID!): Todo
    subtasks: [SubTask]
    subtaskbytodoid(todo_id: ID!): [SubTask]
    subtaskbyid(id: ID!): SubTask
}

input SubTaskInput {
    title: String!
    description: String
}

type Mutation {
    createToDo(title: String!, description: String,completed: Boolean ,subTasks: [SubTaskInput]): Todo
    updateToDo(id: ID!, title: String, description: String, completed: Boolean): Todo
    deleteToDo(id: ID!): Boolean

    createSubTask(todoId: ID!, title: String!, description: String, completed: Boolean): SubTask
    updateSubTask(id: ID!, title: String, description: String, completed: Boolean): SubTask
    deleteSubTask(id: ID!): Boolean
}